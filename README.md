# themesender
- Web app with Firebase Auth, FireStore DB, ParcelJS for bundling and ReactJs as UI

## Starting up
 cpp```
 git clone https://github.com/michaelperryjnr/themsender.git && cd themesender && npm install
```
